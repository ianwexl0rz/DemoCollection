<Grid
    x:Class="DemoCollection.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DemoCollection"
    xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
    d:DesignWidth="1920" d:DesignHeight="1080"
    d:DataContext="{d:DesignInstance {x:Type local:ViewModel}, IsDesignTimeCreatable=True}">

    <noesis:Xaml.Dependencies>
        <noesis:Dependency Source="HudView.xaml"/>
        <noesis:Dependency Source="PauseView.xaml"/>
    </noesis:Xaml.Dependencies>

    <local:HudView x:Name="HudView"/>

    <Viewbox Stretch="Uniform">
        <Grid Height="1080" Width="1920">

            <local:PauseView x:Name="PauseView"/>

            <Grid x:Name="Debug" HorizontalAlignment="Left" Height="241" VerticalAlignment="Bottom" Visibility="Hidden">
                <Button Content="Show Pause Menu" HorizontalAlignment="Left" VerticalAlignment="Bottom" Height="108" Width="299" Command="{Binding PauseViewCommand}"/>
                <Button Content="Show HUD" HorizontalAlignment="Left" Margin="0,0,0,133" VerticalAlignment="Bottom" Height="108" Width="299" Command="{Binding HudViewCommand}"/>
            </Grid>
        </Grid>
    </Viewbox>

</Grid>
