<UserControl x:Class="DemoCollection.HudView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DemoCollection"
             xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">
    <UserControl.DataContext>
        <local:HudViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.Resources>
            <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
        </Grid.Resources>

        <ProgressBar x:Name="healthBar" Margin="32,32,64,0" BorderBrush="#BF000000" RenderTransformOrigin="0.5,0.5" Height="20" Width="512" HorizontalAlignment="Left" VerticalAlignment="Top" Background="#BF000000" Maximum="{Binding Health.Maximum}" Value="{Binding Health.Current}" BorderThickness="2,2,2,2">
            <ProgressBar.Foreground>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Maroon" Offset="1"/>
                    <GradientStop Color="Red"/>
                </LinearGradientBrush>
            </ProgressBar.Foreground>
        </ProgressBar>



        <Ellipse x:Name="reticle" HorizontalAlignment="Left" Height="48" VerticalAlignment="Top" Width="48" StrokeThickness="2" Margin="-24,-24,-24,-24" RenderTransformOrigin="0.5,0.5" Visibility="{Binding Path=HasTarget, Converter={StaticResource BoolToVisConverter} }">
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop Color="White" Offset="0.301"/>
                    <GradientStop Color="#00155368" Offset="0.772"/>
                    <GradientStop Color="#2693E3FF" Offset="0.409"/>
                </RadialGradientBrush>
            </Ellipse.Fill>
            <Ellipse.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform/>
                    <TranslateTransform X="{Binding TargetX}" Y="{Binding TargetY}"/>
                </TransformGroup>
            </Ellipse.RenderTransform>
        </Ellipse>
    </Grid>
</UserControl>
